# OPENROUTER_API_KEY=sk-or-...
# Optional: model slug (default: openai/gpt-4o-mini)
# OPENROUTER_MODEL=openai/gpt-4o-mini

# Recommended: GitHub token — code search (/search/code) requires authentication for reliable results
# GITHUB_TOKEN=ghp_...

# Optional: log AI-selected keys + code search queries + hit counts on the server (verbose in dev logs)
# GITSIMILAR_LOG_AI_OUTPUT=1
